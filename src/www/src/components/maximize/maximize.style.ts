import styled from "styled-components";

export const Button = styled.button`
  outline: none;
  background: transparent;
  border: 0;
  color: white;
  margin-left: auto;

  :hover {
    font-weight: bold;
  }
`;
